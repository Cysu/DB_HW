我的算法的主要框架是基于scancount的，大致流程如下：

1. 算出文档中合法的子串的长度范围，以及索引出现次数的阈值T。
2. 对于每个合法的子串长度，从文档头部开始枚举，计算索引表中出现>=T次的索引。
	1）维护一个合法索引的数组，每次枚举时，相当于删除一些索引并且添加一些新的索引。这个可以在O(L)的时间内维护，其中L是索引数组的长度。
	2）逐项目计算相似度，判断是否确实合法。（这里可以利用相似度函数的三角不等式特性略做优化）。


